const enum BrowserEnum {
  FIREFOX,
  CHROME, // Hasn't been tested since the rework of aran-remote
  PHANTOM_JS,
  // SAFARI:         3, Hasn't been tested since the rework of aran-remote
  // OPERA:          4, Hasn't been tested since the rework of aran-remote
  JSDOM,
  PUPPETEER
}

export default BrowserEnum;